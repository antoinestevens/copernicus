# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

apt_packages:
  - libhdf5-serial-dev
  - libhdf5-dev
  - libgdal-dev
  - libproj-dev


bioc_packages:
  - rhdf5

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
