# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

before_install:
  - libgdal1h
  - gdal-bin
  - libgdal-dev


bioc_packages:
  - rhdf5

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
