# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

install:
  - Rscript -e "install.packages('MODIS', repos='http://R-Forge.R-project.org')"

bioc_packages:
  - rhdf5

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
